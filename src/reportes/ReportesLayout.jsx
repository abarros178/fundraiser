import { Container } from '@mui/material'
import React from 'react'

export const ReportesLayout = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    )
}
